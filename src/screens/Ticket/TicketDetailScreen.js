import React from 'react';
import TicketDetail from '../../Components/Main/Ticket/TicketDetail.js';

const TicketDetailScreen = (props) => {
  return (
    <>
      <TicketDetail {...props} />
    </>
  );
};

export default TicketDetailScreen;